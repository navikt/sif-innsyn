import * as React from 'react';

const NotFoundRoute: React.FC = () => {
    return (
        <div>
            <h1>Siden ikke funnet</h1>
            <p>Beklager, men siden du leter etter eksisterer ikke, eller er flyttet</p>
        </div>
    );
};

export default NotFoundRoute;
